<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.expandenegocio.veonegocio">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivitySinConexion"
            android:label="@string/title_activity_sin_conexion">
            <intent-filter>
                <action android:name="ActivitySinConexion" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityRegistro"
            android:label="@string/title_activity_registro">
            <intent-filter>
                <action android:name="ActivityRegistro" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityServicio"
            android:label="Servicio">
            <intent-filter>
                <action android:name="ActivityServicio" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityPrivacidad"
            android:label="Privacidad">
            <intent-filter>
                <action android:name="ActivityPrivacidad" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityInicioSesion"
            android:label="Inicio Sesion">
            <intent-filter>
                <action android:name="ActivityInicioSesion" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>


        <activity
            android:name=".activities.ActivityConsultas"
            android:label="Consultas"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="ActivityConsultas" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityAltaUsuario"
            android:label="Alta Usuario"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="ActivityAltaUsuario" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityConsultaUsuario"
            android:label="Consulta Usuario"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="ActivityConsultaUsuario" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityOpciones"
            android:label="Opciones"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="ActivityOpciones" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.ActivityFranquiciasProvincias"
            android:label="Franquicias Provincias"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="ActivityFranquiciasProvincias" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>